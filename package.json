{
  "scripts": {
    "build": "mkdir -p dist && cp index.html favicon.ico *.js dist/ && npx javascript-obfuscator ./dist --output ./dist --exclude '*.html' --exclude '*.ico'"
  },
  "devDependencies": {
    "javascript-obfuscator": "^4.1.1"
  }
}